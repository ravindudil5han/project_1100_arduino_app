import 'package:flutter/material.dart';

class AppConHomePage extends StatelessWidget {
  const AppConHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('AppConHomePage')));
  }
}
